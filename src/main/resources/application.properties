spring.application.name=Board2

# application.properties 파일안에서 명령 뒤에 # 주석 쓰지 말 것 - 중요
# Web page Setting : 시작 주소를 /로 설정
server.servlet.context-path=/

# server 의 port 변경
server.port = 8080

# jsp 파일 경로 설정   src/main/wevapp/WEB-INF/views/
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp


# JSP 수정시 서버 재시작없이 바로 적용할 수 있게 설정
server.servlet.jsp.init-parameters.development = true

# mybatis 설정 : src/main/resources/ mapper.xml 경로 설정    /** 뜻 : 하위폴더 포함 모두
mybatis.mapper-locations=classpath:mapper/**/*Mapper.xml

# datasource 설정
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=spring
spring.datasource.password=1234





